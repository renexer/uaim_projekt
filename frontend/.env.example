# W trybie developerskim z backendem na porcie 5000:
REACT_APP_API_URL=http://localhost:5000/api/v1

# W kontenerze frontendowym NGINX proxy przekazuje /api do backendu,
# więc w buildzie Docker można użyć:
# REACT_APP_API_URL=/api/v1
